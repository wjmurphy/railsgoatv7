name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main


jobs:
  codeql:
    name: CodeQL
    uses: alphagov/govuk-infrastructure/.github/workflows/codeql-analysis.yml@codeql-workflows
    permissions:
      security-events: write
